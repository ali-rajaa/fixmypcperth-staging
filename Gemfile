source "https://rubygems.org"

# Plain Jekyll, not the github-pages gem. The github-pages gem pins
# Jekyll 3.10 and force-loads jekyll-github-metadata, which is what
# was breaking the build.
gem "jekyll", "~> 4.3"
gem "webrick"