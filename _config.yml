# FixMyPC Perth — Jekyll configuration
title: FixMyPC Perth
url: https://www.fixmypcperth.com
description: Computer and laptop repair in Canning Vale, Perth.

# ---------------------------------------------------------------------------
# DO NOT add `permalink: pretty`.
# Jekyll's default keeps page.html serving at /page with no trailing slash,
# which is what Google has indexed. Changing it rewrites every URL silently.
# ---------------------------------------------------------------------------

plugins: []

exclude:
  - Gemfile
  - README.md
  - PLAN.md
