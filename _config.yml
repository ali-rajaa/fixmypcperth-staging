title: FixMyPC Perth
description: Computer and laptop repair in Canning Vale, Perth.

# Jekyll's default permalink style keeps page.html serving at /page with
# no trailing slash, which is what Google has indexed. Do not change it.

plugins: []

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - CHECKLIST.md
  - .github