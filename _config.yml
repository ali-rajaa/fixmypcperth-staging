title: FixMyPC Perth
description: Computer and laptop repair in Canning Vale, Perth.

# `url` is deliberately NOT set. GitHub Pages sets it itself, and a manual
# value here is one of the things that can send Liquid into a loop.
# Absolute URLs in the layout are written out in full instead.

theme: null
plugins: []

exclude:
  - Gemfile
  - README.md
  - CHECKLIST.md